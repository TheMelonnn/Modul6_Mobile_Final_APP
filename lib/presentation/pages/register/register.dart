import 'package:flutter/material.dart';

class OldRegisterPage extends StatelessWidget {
  const OldRegisterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Register Page"),
      ),
    );
  }
}
